import * as types from '../actionTypes';


export const openAddUserModal = () => ({
  type: types.OPEN_ADD_USER_MODAL,
});

export const closeAddUserModal = () => ({
  type: types.CLOSE_ADD_USER_MODAL,
});
